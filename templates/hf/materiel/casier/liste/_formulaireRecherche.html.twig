{% import "macros/actions.html.twig" as actions %}

<div class="accordion my-3" id="accordionExample">
	<div class="accordion-item">
		<h2 class="accordion-header">
			<button class="accordion-button enteteAccordion" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
				Formulaire de recherche
			</button>
		</h2>
		<div id="collapseOne" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
			<div class="accordion-body">
				{{ form_start(form)}}
				<div class="row">
					<div class="col-12 col-md-2">{{ form_row(form.agence)}}</div>

					<div class="col-12 col-md-2">
						{{ form_row(form.casier)}}
					</div>
				</div>
				{{ actions.filter_buttons(path('hf_materiel_casier_liste_index')) }}
				{{ form_end(form)}}
			</div>
		</div>
	</div>
</div>
