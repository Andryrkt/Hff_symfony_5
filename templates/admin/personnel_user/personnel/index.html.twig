{% extends 'base.html.twig' %}

{% block title %}Personnel index
{% endblock %}

{% block content %}
	<h1>Personnel index</h1>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Nom</th>
				<th>Prenoms</th>
				<th>Matricule</th>
				<th>Societe</th>
				<th>CreatedAt</th>
				<th>UpdatedAt</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for personnel in personnels %}
				<tr>
					<td>{{ personnel.id }}</td>
					<td>{{ personnel.nom }}</td>
					<td>{{ personnel.prenoms }}</td>
					<td>{{ personnel.matricule }}</td>
					<td>{{ personnel.societe }}</td>
					<td>{{ personnel.createdAt ? personnel.createdAt|date('Y-m-d H:i:s') : '' }}</td>
					<td>{{ personnel.updatedAt ? personnel.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
					<td>
						<a href="{{ path('app_admin_personnel_user_personnel_show', {'id': personnel.id}) }}">show</a>
						<a href="{{ path('app_admin_personnel_user_personnel_edit', {'id': personnel.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="8">pas de donnée trouver</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<a href="{{ path('app_admin_personnel_user_personnel_new') }}">Create new</a>
{% endblock %}
