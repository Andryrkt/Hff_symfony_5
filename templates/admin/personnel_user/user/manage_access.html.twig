{% extends 'base.html.twig' %}

{% block title %}Gérer les accès pour
	{{ user.fullname }}
{% endblock %}

{% block content %}
	<div class="container">
		<div class="d-flex justify-content-between align-items-center mb-4">
			<h1>Gérer les accès pour
				{{ user.fullname }}</h1>
			<a href="{{ path('admin_user_index') }}" class="btn btn-secondary">Retour à la liste</a>
		</div>

		{% include 'admin/personnel_user/user/_access_list.html.twig' with {
            'user': user,
            'accesses': accesses,
            'form': form
        } %}
	</div>
{% endblock %}
