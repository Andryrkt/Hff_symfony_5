{% extends 'base.html.twig' %}

{% block title %}Créer un Type d'Opération{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <h1>Créer un nouveau Type d'Opération</h1>
        </div>
        <div class="card-body">
            {{ include('admin/type_operation/_form.html.twig') }}
        </div>
    </div>

    <a href="{{ path('app_admin_type_operation_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
