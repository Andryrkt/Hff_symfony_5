{% extends 'base.html.twig' %}

{% block title %}UserAccess{% endblock %}

{% block body %}
    <h1>UserAccess</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_access.id }}</td>
            </tr>
            <tr>
                <th>AccessType</th>
                <td>{{ user_access.accessType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_user_access_index') }}">back to list</a>

    <a href="{{ path('admin_user_access_edit', {'id': user_access.id}) }}">edit</a>

    {{ include('user_access/shared/_delete_form.html.twig') }}
{% endblock %}
