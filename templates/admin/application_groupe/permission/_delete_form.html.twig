<form method="post" action="{{ path('app_admin_application_groupe_permission_delete', {'id': permission.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette permission ?');" style="display:inline-block;">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permission.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
