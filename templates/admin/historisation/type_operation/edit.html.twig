{% extends 'base.html.twig' %}

{% block title %}Modifier le Type d'Opération{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h1>Modifier le Type d'Opération</h1>
            {{ include('admin/type_operation/_delete_form.html.twig') }}
        </div>
        <div class="card-body">
            {{ include('admin/type_operation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <a href="{{ path('app_admin_type_operation_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
