<form method="post" action="{{ path('app_admin_type_operation_delete', {'id': type_operation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');" class="d-inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_operation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
