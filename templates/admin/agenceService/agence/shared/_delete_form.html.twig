<form method="post" action="{{ path('admin_agence_delete', {'id': agence.id}) }}" onsubmit="return confirm(' Êtes-vous sûr de vouloir supprimer cette ligne ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agence.id) }}">
    <button class="btn">Delete</button>
</form>
