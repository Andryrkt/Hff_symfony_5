import '../../../../styles/pages/domListe.scss';
import { initAgenceServiceHandlers } from '../../../utils/AgenceServiceManager';

/**
 * Initialise la page de liste des DOM
 */
document.addEventListener('DOMContentLoaded', () => {
    initAgenceServiceHandlers();
});
