framework:
    cache:
        # Unique name of your app: used to compute stable namespaces for cache keys.
        prefix_seed: hff_symfony_5

        # The "app" cache stores to the filesystem by default.
        # The data in this cache should persist between deploys.
        # Other options include:

        # Redis (recommandé pour la production)
        #app: cache.adapter.redis
        #default_redis_provider: redis://localhost

        # APCu (recommandé pour les environnements avec beaucoup de lecture)
        #app: cache.adapter.apcu

        # Namespaced pools use the above "app" backend by default
        pools:
            # Cache dédié pour les requêtes fréquentes
            doctrine.query_cache: null
            doctrine.result_cache: null
            # Cache pour les sessions
            session_cache: null
